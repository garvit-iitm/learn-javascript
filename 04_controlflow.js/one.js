// if-else

isUserLoggedIn = true;

if (isUserLoggedIn) {
  console.log("User is logged in");
}
else {
  console.log("User is not logged in");
}

// == it checks two values for equality
// === it checks two values for equality and same data type